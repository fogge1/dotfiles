# Dotfiles 

## Installation

### Fedora:

Install zsh:

```
sudo dnf install zsh
chsh -s $(which zsh)
```

Clone the repo:
```bash
git clone git@github.com:fogge1/dotfiles.git
cd dotfiles
```

```bash
./install.sh
source ~/.zshrc
```
